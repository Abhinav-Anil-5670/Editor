import React, { useEffect, useRef, useState } from 'react';

const CodeEditor = () => {
  return (
    <div>
        <h2 style={{color: 'red', textAlign: 'center', marginTop: '50px'}}>
            Code Editor should  go here
        </h2>
    </div>
  );
};

export default CodeEditor;